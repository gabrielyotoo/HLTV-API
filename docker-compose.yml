version: '3.8'

services:
  hltv-scraper:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hltv-scraper
    restart: unless-stopped
    volumes:
      # Mount keywords-news.txt so you can update it without rebuilding
      - ./keywords-news.txt:/app/keywords-news.txt:ro
    # Uncomment to add environment variables if needed
    # environment:
    #   - LOG_LEVEL=info
